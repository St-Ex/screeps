module.exports = class HarvestController {
    
    constructor(max) {
        this.maxcreeps=max;
    }
    
    control(){

        if (Game.creeps.filter())
        console.log(Game.creeps);
    }
}